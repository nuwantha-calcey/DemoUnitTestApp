name: "Pull Request Labeler"

on:
  - pull_request

jobs:
  triage:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
    - name: Code checkout
      uses: actions/checkout@v3
    - uses: actions/labeler@v4
      with:
        sync-labels: true